defmodule Membrane.ERTMP do
  @moduledoc """
  Membrane plugin for publishing audio/video streams via Enhanced RTMP (E-RTMP).

  The main entry point is `Membrane.ERTMP.Sink`.

  ## Requirements

  * Rust 1.85 or later (the underlying `rtmp` crate uses Rust edition 2024).
  * `cargo` available on `$PATH` during compilation.
  """
end
